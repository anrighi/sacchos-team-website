# Override dello Sheet rosa (ingest a build). La rosa resta su Google Drive.
# Senza questa variabile si usa src/data/roster.sheet.url (link condiviso).
ROSTER_SHEET_CSV_URL=

# Deploy Cloudflare (CI). Vedi docs/CLOUDFLARE.md
# CLOUDFLARE_API_TOKEN=
# CLOUDFLARE_ACCOUNT_ID=
